import React from 'react'

const Button = ({ button }) => {
    return (
        <>
            <button className='bg-gray-200 text-xs text-gray-600 uppercase tracking-widest font-bold px-6 py-3 rounded hover:bg-teal-500 hover:text-white focus:outline-none'>Free</button>
        </>
    )
}

export default Button
