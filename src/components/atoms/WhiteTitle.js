import React from 'react'

const WhiteTitle = ({ whitetitle }) => {
    return (
        <div>
            <a href='/' className='absolute text-white font-bold z-10 pl-2 pt-1'>{whitetitle}</a>
        </div>
    )
}

export default WhiteTitle
